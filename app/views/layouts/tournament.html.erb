<%= content_for(:content) do %>
  <section class="flex flex-col md:px-32 items-center">
    <div class="flex flex-col gap-8">
      <%= render("tournament/rounds/top_nav", tournament:) %>

      <%= yield %>

      <%= render("tournament/rounds/bottom_nav", tournament:) %>
    </div>
  </section>
<% end %>

<%= render template: "layouts/application" %>
